    <nav class="navbar navbar-expand-xl navbar-dark bg-dark">
        <a href="{% url 'home' %}" class="navbar-brand"><i class="fa fa-dollar"></i>Budget<b>Helper</b></a>
        {% if user.is_authenticated %}
        <button type="button" class="navbar-toggler col-2" data-toggle="collapse" data-target="#navbarCollapse"
            style="padding: 0;">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div id="navbarCollapse" class="collapse navbar-collapse justify-content-start">

            <div class="navbar-nav ml-auto">
                <a href="{% url 'home' %}" class="nav-item nav-link d-flex flex-lg-column">
                    <i class="fa fa-home mx-lg-auto "></i>
                    <p class="mb-lg-0 mt-lg-2 my-auto">Home</p>
                </a>
                <a href="{% url 'cat_list' %}" class="nav-item nav-link d-flex flex-lg-column">
                    <i class="fa fa-pie-chart mx-lg-auto"></i>
                    <p class="mb-lg-0 mt-lg-2">Analytics</p>
                </a>
                <div class="nav-item dropdown col-6">
                    <a href="#" data-toggle="dropdown" class="nav-item nav-link dropdown-toggle user-action">
                        <img sizes="21,12" src="{{ user.profile_pic.url }}" class="avatar" alt="Avatar">
                        <i class="caret"></i>
                        <span>{{user.username}}</span>
                    </a>
                    <div class="dropdown-menu" style="left:-20px;overflow: hidden;">
                        <a href="{% url 'edit_profile' user.pk %}" class="dropdown-item"><i class="fa fa-user"></i>
                            Profile
                        </a>
                        <a href="#" class="dropdown-item" onclick="cat_add()"><i class="fa fa-plus"></i> Add
                            Category
                        </a>
                        <hr>
                        <a href="{% url 'logout' %}" class="dropdown-item"><i class=" fa fa-power-off"
                                style="font-size: large;"></i>
                            Logout
                        </a>
                    </div>
                </div>
            </div>
        </div>
        {% else %}
        <!-- <div id="navbarCollapse" class="collapse navbar-collapse justify-content-start"> -->

        <div class="ml-auto" style="display: inline-block;">
            <a href="{% url 'signup' %}"><button class="btn" id="signup"
                    style="border: 1px solid black;">Signup</button></a>
            <a href="{% url 'login' %}"><button class="btn btn-primary">Login</button></a>
        </div>
        <!-- </div> -->
        {%endif%}
    </nav>